# ==============================================
# DEVELOPMENT ENVIRONMENT VARIABLES
# ==============================================
# Copy this file to .env for local development
# These are safe defaults for development only!

# Database Configuration (Development)
POSTGRES_USER=telegram_secrets
POSTGRES_PASSWORD=devpassword123
POSTGRES_DB=telegram_secrets_db
DATABASE_URL=postgresql://telegram_secrets:devpassword123@localhost:5433/telegram_secrets_db

# JWT Configuration (Development - DO NOT use in production!)
JWT_SECRET=dev_jwt_secret_this_is_not_secure_for_production_only

# PushinPay Configuration (Use sandbox/test credentials)
PUSHINPAY_TOKEN=your_pushinpay_sandbox_token_here

# Application URLs (Development)
FRONTEND_URL=http://localhost:3000
BACKEND_URL=http://localhost:3001
NEXT_PUBLIC_API_URL=http://localhost:3001
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Node Environment
NODE_ENV=development

# Server Ports
PORT=3001

# ==============================================
# QUICK START
# ==============================================
# 1. Copy this file: cp .env.development.example backend/.env
# 2. Start database: docker-compose -f docker-compose.dev.yml up -d
# 3. Run migrations: cd backend && npx prisma migrate dev
# 4. Start backend: cd backend && npm run dev
# 5. Start frontend: cd frontend && npm run dev
